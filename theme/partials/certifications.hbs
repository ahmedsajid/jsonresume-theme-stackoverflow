{{#if resume.certifications.length}}
<section class="section">
  <header>
    <h2 class='section-title'>Certification <span class="item-count">({{resume.certifications.length}})</span></h2>
  </header>
  <section id="certifications">
    {{#each resume.certifications}}
    <section class="certification-item">
      <label for="certification-item-{{@index}}"></label>
      <header class="clear">
        <div class="header-left">
          {{#if name}}
          <div class="title">
            {{name}}
          </div>
          {{/if}}
          {{#if issuer}}
          <div class="issuer">
            {{issuer}}
          </div>
          {{/if}}
        </div>

        {{#if date}}
        <div class="date">
          {{Y date}}
        </div>
        {{/if}}
      </header>
			{{#if url}}
    	<span class="website">
    		<a target="_blank" href="{{url}}">{{url}}</a>
    	</span>
      {{/if}}
    </section>
    {{/each}}
  </section>
</section>
{{/if}}
